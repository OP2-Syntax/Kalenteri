import type { RichTreeViewState } from "../../RichTreeViewStore/index.js";
import type { TreeViewItemId } from "../../../models/index.js";
export declare const labelSelectors: {
  /**
   * Checks whether an item is editable.
   */
  isItemEditable: (state: RichTreeViewState<any, any>, itemId: TreeViewItemId) => boolean;
  /**
   * Checks whether an item is being edited.
   */
  isItemBeingEdited: (state: RichTreeViewState<any, any>, itemId: TreeViewItemId | null) => boolean;
  /**
   * Checks whether any item is being edited.
   */
  isAnyItemBeingEdited: (state: RichTreeViewState<any, any>) => boolean;
};