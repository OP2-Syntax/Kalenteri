import * as React from 'react';
import type { DayViewProps } from "./DayView.types.mjs";
/**
 * A Day View to use inside the Event Calendar.
 *
 * Events adapt to the device on their own (mouse vs. touch), so no view-level config is needed.
 */
export declare const DayView: React.NamedExoticComponent<DayViewProps & React.RefAttributes<HTMLDivElement>>;