'use client';

import { useEventDialogFormContext } from "../internals/components/event-dialog/form/EventDialogFormContext.mjs";

/**
 * Returns the occurrence the event dialog is editing.
 * Constant for the lifetime of the editing session.
 */
export function useEventDialogOccurrence() {
  return useEventDialogFormContext().occurrence;
}