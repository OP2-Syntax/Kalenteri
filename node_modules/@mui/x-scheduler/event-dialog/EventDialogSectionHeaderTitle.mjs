'use client';

import _extends from "@babel/runtime/helpers/esm/extends";
import * as React from 'react';
import PropTypes from 'prop-types';
import clsx from 'clsx';
import { styled } from '@mui/material/styles';
import { useEventEditingStyledContext } from "../internals/components/event-editing/EventEditingStyledContext.mjs";
import { jsx as _jsx } from "react/jsx-runtime";
const SectionHeaderTitleRoot = styled('legend', {
  name: 'MuiEventDialog',
  slot: 'SectionHeaderTitle'
})(({
  theme
}) => _extends({}, theme.typography.subtitle2, {
  padding: 0,
  marginBlockEnd: theme.spacing(2),
  textTransform: 'uppercase',
  color: (theme.vars || theme).palette.text.secondary
}));

/**
 * Section title of the event dialog form, rendered as the fieldset's legend.
 */
const EventDialogSectionHeaderTitle = /*#__PURE__*/React.forwardRef(function EventDialogSectionHeaderTitle(props, forwardedRef) {
  const {
    classes
  } = useEventEditingStyledContext();
  return /*#__PURE__*/_jsx(SectionHeaderTitleRoot, _extends({}, props, {
    ref: forwardedRef,
    className: clsx(classes.eventDialogSectionHeaderTitle, props.className)
  }));
});
if (process.env.NODE_ENV !== "production") EventDialogSectionHeaderTitle.displayName = "EventDialogSectionHeaderTitle";
process.env.NODE_ENV !== "production" ? EventDialogSectionHeaderTitle.propTypes /* remove-proptypes */ = {
  // ----------------------------- Warning --------------------------------
  // | These PropTypes are generated from the TypeScript type definitions |
  // | To update them edit the TypeScript types and run "pnpm proptypes"  |
  // ----------------------------------------------------------------------
  sx: PropTypes.oneOfType([PropTypes.arrayOf(PropTypes.oneOfType([PropTypes.func, PropTypes.object, PropTypes.bool])), PropTypes.func, PropTypes.object])
} : void 0;
export { EventDialogSectionHeaderTitle };