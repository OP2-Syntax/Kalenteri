'use client';

import _extends from "@babel/runtime/helpers/esm/extends";
import * as React from 'react';
import PropTypes from 'prop-types';
import clsx from 'clsx';
import { styled } from '@mui/material/styles';
import { useEventEditingStyledContext } from "../internals/components/event-editing/EventEditingStyledContext.mjs";
import { jsx as _jsx } from "react/jsx-runtime";
const SectionFieldsetRoot = styled('fieldset', {
  name: 'MuiEventDialog',
  slot: 'SectionFieldset'
})(({
  theme
}) => ({
  display: 'flex',
  flexDirection: 'column',
  gap: theme.spacing(2),
  border: 0,
  margin: 0,
  padding: 0,
  minInlineSize: 'min-content'
}));

/**
 * Layout wrapper for one section of the event dialog form.
 * Applies the section spacing and the theme classes, so custom sections look native.
 */
const EventDialogSectionFieldset = /*#__PURE__*/React.forwardRef(function EventDialogSectionFieldset(props, forwardedRef) {
  const {
    classes
  } = useEventEditingStyledContext();
  return /*#__PURE__*/_jsx(SectionFieldsetRoot, _extends({}, props, {
    ref: forwardedRef,
    className: clsx(classes.eventDialogSectionFieldset, props.className)
  }));
});
if (process.env.NODE_ENV !== "production") EventDialogSectionFieldset.displayName = "EventDialogSectionFieldset";
process.env.NODE_ENV !== "production" ? EventDialogSectionFieldset.propTypes /* remove-proptypes */ = {
  // ----------------------------- Warning --------------------------------
  // | These PropTypes are generated from the TypeScript type definitions |
  // | To update them edit the TypeScript types and run "pnpm proptypes"  |
  // ----------------------------------------------------------------------
  sx: PropTypes.oneOfType([PropTypes.arrayOf(PropTypes.oneOfType([PropTypes.func, PropTypes.object, PropTypes.bool])), PropTypes.func, PropTypes.object])
} : void 0;
export { EventDialogSectionFieldset };