import * as React from 'react';
import type { WeekViewProps } from "./WeekView.types.js";
/**
 * A Week View to use inside the Event Calendar.
 *
 * Events adapt to the device on their own (mouse vs. touch), so no view-level config is needed.
 */
export declare const WeekView: React.NamedExoticComponent<WeekViewProps & React.RefAttributes<HTMLDivElement>>;