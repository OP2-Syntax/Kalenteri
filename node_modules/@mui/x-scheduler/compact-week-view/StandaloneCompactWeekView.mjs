'use client';

import _extends from "@babel/runtime/helpers/esm/extends";
import _objectWithoutPropertiesLoose from "@babel/runtime/helpers/esm/objectWithoutPropertiesLoose";
const _excluded = ["localeText", "slots", "slotProps"];
import * as React from 'react';
import { useThemeProps } from '@mui/material/styles';
import { useExtractEventCalendarParameters } from '@mui/x-scheduler-internals/use-event-calendar';
import { EventCalendarProvider } from "../internals/components/EventCalendarProvider.mjs";
import { EventDialogProvider } from "../internals/components/event-dialog/index.mjs";
import { ResponsiveTypographyContainer } from "../internals/components/ResponsiveTypographyContainer.mjs";
import { CompactWeekView } from "./CompactWeekView.mjs";

/**
 * A touch-optimized Week View (7 days) for narrow widths that can be used outside of the
 * Event Calendar.
 */
import { jsx as _jsx } from "react/jsx-runtime";
const StandaloneCompactWeekView = /*#__PURE__*/React.forwardRef(function StandaloneCompactWeekView(inProps, forwardedRef) {
  // eslint-disable-next-line mui/material-ui-name-matches-component-name
  const props = useThemeProps({
    props: inProps,
    name: 'MuiEventCalendar'
  });
  const {
    parameters,
    forwardedProps
  } = useExtractEventCalendarParameters(props);
  const {
      localeText,
      slots,
      slotProps
    } = forwardedProps,
    other = _objectWithoutPropertiesLoose(forwardedProps, _excluded);
  return /*#__PURE__*/_jsx(ResponsiveTypographyContainer, {
    children: /*#__PURE__*/_jsx(EventCalendarProvider, _extends({}, parameters, {
      localeText: localeText,
      slots: slots,
      slotProps: slotProps,
      children: /*#__PURE__*/_jsx(EventDialogProvider, {
        children: /*#__PURE__*/_jsx(CompactWeekView, _extends({
          ref: forwardedRef
        }, other))
      })
    }))
  });
});
if (process.env.NODE_ENV !== "production") StandaloneCompactWeekView.displayName = "StandaloneCompactWeekView";
export { StandaloneCompactWeekView };