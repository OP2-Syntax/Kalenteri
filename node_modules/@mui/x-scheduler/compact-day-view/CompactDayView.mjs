'use client';

import _extends from "@babel/runtime/helpers/esm/extends";
import * as React from 'react';
import { useStore } from '@base-ui/utils/store';
import { useEventCalendarView } from '@mui/x-scheduler-internals/use-event-calendar-view';
import { useEventCalendarStoreContext } from '@mui/x-scheduler-internals/use-event-calendar-store-context';
import { eventCalendarViewSelectors } from '@mui/x-scheduler-internals/event-calendar-selectors';
import { CompactDayTimeGrid } from "../internals/components/compact-day-time-grid/index.mjs";
import { createDayTimeGridViewDefinition } from "../internals/utils/day-time-grid-view-definition.mjs";
import { jsx as _jsx } from "react/jsx-runtime";
const COMPACT_DAY_VIEW_DEFINITION = createDayTimeGridViewDefinition(1);

/**
 * A touch-optimized Day View (1 day) for narrow widths, to use inside the Event Calendar.
 */
export const CompactDayView = /*#__PURE__*/React.memo(/*#__PURE__*/React.forwardRef(function CompactDayView(props, forwardedRef) {
  // Context hooks
  const store = useEventCalendarStoreContext();

  // Feature hooks
  const {
    days
  } = useEventCalendarView(COMPACT_DAY_VIEW_DEFINITION);

  // Selector hooks
  const config = useStore(store, eventCalendarViewSelectors.timeGridConfig, 'day');
  return /*#__PURE__*/_jsx(CompactDayTimeGrid, _extends({
    ref: forwardedRef,
    days: days,
    startTime: config?.startTime,
    endTime: config?.endTime,
    hourRangeSource: "viewConfig.day"
  }, props));
}));
if (process.env.NODE_ENV !== "production") CompactDayView.displayName = "CompactDayView";