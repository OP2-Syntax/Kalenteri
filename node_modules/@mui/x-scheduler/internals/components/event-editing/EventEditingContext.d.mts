import * as React from 'react';
import type { SchedulerRenderableEventOccurrence } from '@mui/x-scheduler-internals/models';
import type { CompactEventEditingProviderProps, EventEditingContextValue, EventEditingProviderProps, EventEditingTriggerProps } from "./EventEditing.types.mjs";
export declare const EventEditingContext: React.Context<EventEditingContextValue | undefined>;
export declare function useEventEditingContext(): EventEditingContextValue;
/**
 * Surface-agnostic editing backbone. Editing state (which occurrence, which stage) lives on the store;
 * this context only adds the anchor element and the start/stop helpers. Consumers provide the surface.
 */
export declare function EventEditingProvider(props: EventEditingProviderProps): React.JSX.Element;
/**
 * Ref, anchoring, and `startEditing` wiring shared by every trigger that edits an occurrence
 * (`EventEditingTrigger` below and `EventContextMenuTrigger`), so they stay in lockstep instead of
 * duplicating the re-anchoring effect.
 */
export declare function useEventEditingTriggerProps(occurrence: SchedulerRenderableEventOccurrence, options?: {
  onEditingCanceled?: () => void;
  stableAnchor?: HTMLElement | null;
}): {
  ref: React.RefObject<HTMLElement | null>;
  onClick: (event: React.MouseEvent<HTMLElement>) => void;
};
/**
 * Wraps an element so activating it edits its occurrence and opens the editing surface. Works for
 * both the desktop dialog and the compact drawer.
 */
export declare function EventEditingTrigger(props: EventEditingTriggerProps): React.ReactElement<any, string | React.JSXElementConstructor<any>>;
/**
 * Compact (mobile) editing surface: reuses the backbone but renders no surface of its own. The
 * drawer is rendered in-flow by `CompactDayTimeGrid`, reading editing state from the store.
 */
export declare function CompactEventEditingProvider(props: CompactEventEditingProviderProps): React.JSX.Element;