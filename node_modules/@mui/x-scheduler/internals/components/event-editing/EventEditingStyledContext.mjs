'use client';

import _formatErrorMessage from "@mui/x-internals/formatErrorMessage";
import * as React from 'react';
export const EventEditingStyledContext = /*#__PURE__*/React.createContext(null);
if (process.env.NODE_ENV !== "production") EventEditingStyledContext.displayName = "EventEditingStyledContext";
export function useEventEditingStyledContext() {
  const value = React.useContext(EventEditingStyledContext);
  if (!value) {
    throw new Error(process.env.NODE_ENV !== "production" ? 'MUI X Scheduler: useEventEditingStyledContext must be used within an EventEditingStyledContext.Provider. ' + 'The component requires access to the editing surface styling and locale information. ' + 'Ensure the component is rendered inside an EventCalendar (or premium) provider.' : _formatErrorMessage(207));
  }
  return value;
}