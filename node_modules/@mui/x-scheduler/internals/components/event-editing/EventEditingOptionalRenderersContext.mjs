'use client';

import * as React from 'react';

/**
 * Components optionally rendered inside the editing surface. Premium fills these via
 * `optionalRenderers`; community leaves them empty.
 */

export const EventEditingOptionalRenderersContext = /*#__PURE__*/React.createContext({});
if (process.env.NODE_ENV !== "production") EventEditingOptionalRenderersContext.displayName = "EventEditingOptionalRenderersContext";
export function useEventEditingOptionalRenderers() {
  return React.useContext(EventEditingOptionalRenderersContext);
}