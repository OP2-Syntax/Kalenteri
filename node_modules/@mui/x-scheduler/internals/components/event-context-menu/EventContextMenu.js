"use strict";
'use client';

var _interopRequireDefault = require("@babel/runtime/helpers/interopRequireDefault").default;
var _interopRequireWildcard = require("@babel/runtime/helpers/interopRequireWildcard").default;
Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.EventContextMenu = EventContextMenu;
var React = _interopRequireWildcard(require("react"));
var _Menu = _interopRequireDefault(require("@mui/material/Menu"));
var _eventEditing = require("../event-editing");
var _EventContextMenuItems = require("./EventContextMenuItems");
var _jsxRuntime = require("react/jsx-runtime");
/**
 * The menu shown on right-click of an event, or on pressing `Space` while it is focused. Thin
 * presentational wrapper around `@mui/material/Menu` — all the actual logic lives in
 * `useEventContextMenuItems`.
 */
function EventContextMenu(props) {
  const {
    open,
    occurrence,
    anchorEl,
    anchorPosition,
    onEditingCanceled,
    stableAnchor,
    onClose
  } = props;
  const {
    schedulerId,
    classes,
    localeText
  } = (0, _eventEditing.useEventEditingStyledContext)();
  const items = (0, _EventContextMenuItems.useEventContextMenuItems)({
    occurrence,
    anchorEl,
    onRequestClose: onClose,
    onEditingCanceled,
    stableAnchor
  });
  return /*#__PURE__*/(0, _jsxRuntime.jsx)(_Menu.default, {
    className: classes.eventContextMenu,
    id: schedulerId ? `${schedulerId}-event-context-menu` : undefined,
    open: open,
    onClose: onClose,
    anchorReference: anchorPosition ? 'anchorPosition' : 'anchorEl',
    anchorPosition: anchorPosition ?? undefined,
    anchorEl: anchorPosition ? undefined : anchorEl,
    slotProps: {
      list: {
        'aria-label': localeText.eventContextMenuAriaLabel
      }
    },
    children: items
  });
}