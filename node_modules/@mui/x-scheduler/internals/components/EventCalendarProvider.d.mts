import * as React from 'react';
import { EventCalendarProvider as UnstyledEventCalendarProvider } from '@mui/x-scheduler-internals/event-calendar-provider';
import type { EventCalendarLocaleText } from "../../models/translations.mjs";
import type { SchedulerSlotsAndSlotProps } from "../../models/slots.mjs";
export interface EventCalendarProviderProps<TEvent extends object, TResource extends object> extends UnstyledEventCalendarProvider.Props<TEvent, TResource>, SchedulerSlotsAndSlotProps {
  /**
   * Set the locale text of the view.
   * You can find all the translation keys supported in [the source](https://github.com/mui/mui-x/blob/HEAD/packages/x-scheduler/src/models/translations.ts)
   * in the GitHub repository.
   */
  localeText?: Partial<EventCalendarLocaleText>;
}
export declare function EventCalendarProvider<TEvent extends object, TResource extends object>(props: EventCalendarProviderProps<TEvent, TResource>): React.JSX.Element;