'use client';

import * as React from 'react';
import { styled } from '@mui/material/styles';
import Typography from '@mui/material/Typography';
import IconButton from '@mui/material/IconButton';
import CloseRounded from '@mui/icons-material/CloseRounded';
import { useEventEditingStyledContext, ReadonlyEventDetails } from "../event-editing/index.mjs";
import { jsx as _jsx, jsxs as _jsxs } from "react/jsx-runtime";
const CompactReadonlyContentHeader = styled('header', {
  name: 'MuiEventCalendar',
  slot: 'CompactEventDrawerReadonlyHeader'
})(({
  theme
}) => ({
  display: 'flex',
  alignItems: 'center',
  justifyContent: 'space-between',
  gap: theme.spacing(1),
  padding: theme.spacing(0.5, 3)
}));
const CompactReadonlyContentTitle = styled(Typography, {
  name: 'MuiEventCalendar',
  slot: 'CompactEventDrawerReadonlyTitle'
})({
  flex: 1,
  minWidth: 0,
  margin: 0,
  color: 'var(--event-on-surface-subtle-secondary)',
  cursor: 'default'
});
/**
 * The read-only summary shown inside the compact drawer: the shared `ReadonlyEventDetails` with
 * lighter chrome (small heading + close button, no footer).
 */
export function CompactReadonlyContent(props) {
  const {
    occurrence,
    onClose
  } = props;

  // Context hooks
  const {
    localeText,
    classes
  } = useEventEditingStyledContext();
  return /*#__PURE__*/_jsxs(React.Fragment, {
    children: [/*#__PURE__*/_jsxs(CompactReadonlyContentHeader, {
      className: classes.compactEventDrawerReadonlyHeader,
      children: [/*#__PURE__*/_jsx(CompactReadonlyContentTitle, {
        className: classes.compactEventDrawerReadonlyTitle,
        variant: "body1",
        noWrap: true,
        children: occurrence.title
      }), /*#__PURE__*/_jsx(IconButton, {
        size: "small",
        edge: "end",
        "aria-label": localeText.closeButtonAriaLabel,
        onClick: onClose,
        children: /*#__PURE__*/_jsx(CloseRounded, {
          fontSize: "small"
        })
      })]
    }), /*#__PURE__*/_jsx(ReadonlyEventDetails, {
      occurrence: occurrence
    })]
  });
}