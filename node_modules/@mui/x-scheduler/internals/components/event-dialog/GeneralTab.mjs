'use client';

import _extends from "@babel/runtime/helpers/esm/extends";
import * as React from 'react';
import { useEventEditingStyledContext } from "../event-editing/index.mjs";
import { useSchedulerSlots } from "../SchedulerSlotsContext.mjs";
import { EventDialogGeneralTabContent } from "../../../event-dialog/EventDialogGeneralTabContent.mjs";
import { EventDialogTabPanel, EventDialogTabContent } from "./EventDialogTabPanel.mjs";
import { jsx as _jsx } from "react/jsx-runtime";
export function GeneralTab(props) {
  const {
    value,
    hasTabs
  } = props;
  const {
    schedulerId,
    classes
  } = useEventEditingStyledContext();
  const {
    slots,
    slotProps
  } = useSchedulerSlots();

  // The tab panel stays owned by the dialog: its `hidden` state and its `aria-labelledby`
  // pairing with the tab are not reachable from the slot.
  const GeneralTabContent = slots.eventDialogGeneralTab ?? EventDialogGeneralTabContent;
  return /*#__PURE__*/_jsx(EventDialogTabPanel, {
    role: hasTabs ? 'tabpanel' : undefined,
    id: hasTabs ? `${schedulerId}-general-tabpanel` : undefined,
    "aria-labelledby": hasTabs ? `${schedulerId}-general-tab` : undefined,
    className: classes.eventDialogTabPanel,
    hidden: value !== 'general',
    children: /*#__PURE__*/_jsx(EventDialogTabContent, {
      className: classes.eventDialogTabContent,
      children: /*#__PURE__*/_jsx(GeneralTabContent, _extends({}, slotProps.eventDialogGeneralTab))
    })
  });
}