'use client';

import * as React from 'react';
import { styled } from '@mui/material/styles';
import { inputBaseClasses } from '@mui/material/InputBase';
import TextField from '@mui/material/TextField';
import { useEventEditingStyledContext } from "../event-editing/index.mjs";
import { useEventDialogFormContext } from "./form/EventDialogFormContext.mjs";
import { useEventDialogFormField } from "../../../event-dialog/useEventDialogFormField.mjs";
import { jsx as _jsx, jsxs as _jsxs } from "react/jsx-runtime";
const TitleTextField = styled(TextField, {
  name: 'MuiEventDialog',
  slot: 'TitleTextField'
})(({
  theme
}) => ({
  flex: 1,
  [`& .${inputBaseClasses.root}`]: {
    fontSize: theme.typography.h6.fontSize,
    lineHeight: theme.typography.h6.lineHeight,
    fontWeight: theme.typography.h6.fontWeight
  }
}));

// Rendering the title in its own section keeps typing from re-rendering the
// whole dialog content: only this component subscribes to the `title` field.
export default function TitleSection() {
  // Context hooks
  const {
    occurrence
  } = useEventDialogFormContext();
  const {
    schedulerId,
    localeText
  } = useEventEditingStyledContext();
  const title = useEventDialogFormField('title');
  // `preventScroll` so focusing the title doesn't scroll a still-off-screen drawer into view
  // (the compact drawer slides up from the bottom, which would otherwise shove the grid).
  const titleInputRef = React.useCallback(input => input?.focus({
    preventScroll: true
  }), []);
  return /*#__PURE__*/_jsxs(React.Fragment, {
    children: [/*#__PURE__*/_jsx("span", {
      id: `${schedulerId}-event-dialog-title`,
      style: {
        position: 'absolute',
        width: 0,
        height: 0,
        overflow: 'hidden'
      },
      children: occurrence.title
    }), /*#__PURE__*/_jsx(TitleTextField, {
      name: "title",
      value: title.value,
      onChange: event => title.setValue(event.target.value),
      required: true,
      inputRef: titleInputRef,
      slotProps: {
        input: {
          readOnly: title.readOnly,
          'aria-label': localeText.eventTitleAriaLabel
        },
        formHelperText: {
          role: 'alert'
        }
      },
      error: !!title.error,
      helperText: title.error,
      fullWidth: true,
      size: "small"
    })]
  });
}