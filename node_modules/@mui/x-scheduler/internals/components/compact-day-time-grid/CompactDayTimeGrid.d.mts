import * as React from 'react';
import type { DayTimeGridProps } from "../day-time-grid/DayTimeGrid.types.mjs";
/**
 * Shared layout for the compact (touch) views. Reuses `DayTimeGrid`; arming an event docks a toolbar
 * at the bottom, whose Edit opens the drawer overlaid on the view. Each view provides its own `days`.
 */
export declare const CompactDayTimeGrid: React.ForwardRefExoticComponent<DayTimeGridProps & React.RefAttributes<HTMLDivElement>>;