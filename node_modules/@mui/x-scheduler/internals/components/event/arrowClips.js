"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.getArrowFocusVisibleStyles = exports.RIGHT_ARROW_CLIP = exports.LEFT_ARROW_CLIP = exports.BOTH_ARROWS_CLIP = exports.ARROW_DEPTH = void 0;
const ARROW_DEPTH = exports.ARROW_DEPTH = 8; // px - depth of the chevron point
const LEFT_ARROW_CLIP = exports.LEFT_ARROW_CLIP = `polygon(${ARROW_DEPTH}px 0, 100% 0, 100% 100%, ${ARROW_DEPTH}px 100%, 0 50%)`;
const RIGHT_ARROW_CLIP = exports.RIGHT_ARROW_CLIP = `polygon(0 0, calc(100% - ${ARROW_DEPTH}px) 0, 100% 50%, calc(100% - ${ARROW_DEPTH}px) 100%, 0 100%)`;
const BOTH_ARROWS_CLIP = exports.BOTH_ARROWS_CLIP = `polygon(${ARROW_DEPTH}px 0, calc(100% - ${ARROW_DEPTH}px) 0, 100% 50%, calc(100% - ${ARROW_DEPTH}px) 100%, ${ARROW_DEPTH}px 100%, 0 50%)`;

/**
 * The chevron clip also clips the focus outline away, so focused events fall back to a plain
 * rounded rectangle.
 */
const getArrowFocusVisibleStyles = borderRadius => ({
  '&[data-starting-before-edge]:focus-visible, &[data-ending-after-edge]:focus-visible': {
    clipPath: 'none',
    borderRadius
  }
});
exports.getArrowFocusVisibleStyles = getArrowFocusVisibleStyles;