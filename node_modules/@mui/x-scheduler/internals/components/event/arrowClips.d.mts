export declare const ARROW_DEPTH = 8;
export declare const LEFT_ARROW_CLIP = "polygon(8px 0, 100% 0, 100% 100%, 8px 100%, 0 50%)";
export declare const RIGHT_ARROW_CLIP = "polygon(0 0, calc(100% - 8px) 0, 100% 50%, calc(100% - 8px) 100%, 0 100%)";
export declare const BOTH_ARROWS_CLIP = "polygon(8px 0, calc(100% - 8px) 0, 100% 50%, calc(100% - 8px) 100%, 8px 100%, 0 50%)";
/**
 * The chevron clip also clips the focus outline away, so focused events fall back to a plain
 * rounded rectangle.
 */
export declare const getArrowFocusVisibleStyles: (borderRadius: number | string) => {
  '&[data-starting-before-edge]:focus-visible, &[data-ending-after-edge]:focus-visible': {
    clipPath: string;
    borderRadius: string | number;
  };
};