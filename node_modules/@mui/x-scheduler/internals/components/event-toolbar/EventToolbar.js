"use strict";
'use client';

var _interopRequireDefault = require("@babel/runtime/helpers/interopRequireDefault").default;
var _interopRequireWildcard = require("@babel/runtime/helpers/interopRequireWildcard").default;
Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.EventToolbar = EventToolbar;
var React = _interopRequireWildcard(require("react"));
var _styles = require("@mui/material/styles");
var _Paper = _interopRequireDefault(require("@mui/material/Paper"));
var _IconButton = _interopRequireDefault(require("@mui/material/IconButton"));
var _EditRounded = _interopRequireDefault(require("@mui/icons-material/EditRounded"));
var _DeleteRounded = _interopRequireDefault(require("@mui/icons-material/DeleteRounded"));
var _store = require("@base-ui/utils/store");
var _schedulerSelectors = require("@mui/x-scheduler-internals/scheduler-selectors");
var _useSchedulerStoreContext = require("@mui/x-scheduler-internals/use-scheduler-store-context");
var _eventEditing = require("../event-editing");
var _armedOccurrence = require("../armed-occurrence");
var _jsxRuntime = require("react/jsx-runtime");
// `Paper` (elevation 3) supplies the `background.paper` fill and `shadows[3]` box shadow.
const EventToolbarRoot = (0, _styles.styled)(_Paper.default, {
  name: 'MuiEventCalendar',
  slot: 'Toolbar'
})(({
  theme
}) => ({
  display: 'inline-flex',
  alignItems: 'center',
  gap: theme.spacing(0.5),
  padding: theme.spacing(0.5),
  borderRadius: 50,
  border: `1px solid ${(theme.vars || theme).palette.divider}`
}));
const EventToolbarButton = (0, _styles.styled)(_IconButton.default, {
  name: 'MuiEventCalendar',
  slot: 'ToolbarButton'
})(({
  theme
}) => ({
  width: 40,
  height: 40,
  padding: 0,
  // Circular icon button to match the pill-shaped toolbar container.
  borderRadius: '50%',
  color: (theme.vars || theme).palette.text.primary,
  '&:hover': {
    backgroundColor: (theme.vars || theme).palette.action.hover
  },
  '&:focus-visible': {
    outline: `2px solid ${(theme.vars || theme).palette.primary.main}`,
    outlineOffset: -2
  }
}));
function EventToolbar(props) {
  const {
    occurrence
  } = props;
  const store = (0, _useSchedulerStoreContext.useSchedulerStoreContext)();
  const {
    stopEditing,
    anchor,
    stableAnchorRef
  } = (0, _eventEditing.useEventEditingContext)();
  const {
    classes,
    localeText
  } = (0, _eventEditing.useEventEditingStyledContext)();
  const recurringEventsPlugin = (0, _store.useStore)(store, _schedulerSelectors.schedulerOtherSelectors.recurringEventsPlugin);
  const areRecurringEventsAvailable = (0, _store.useStore)(store, _schedulerSelectors.schedulerOtherSelectors.areRecurringEventsAvailable);

  // Rendered only while armed (desktop anchored surface + mobile dock), so Escape here disarms both.
  (0, _armedOccurrence.useDisarmOnEscape)({
    active: true,
    onDisarm: stopEditing
  });
  const handleEdit = event => {
    // A canceled edit disarms and unmounts this toolbar, so the activation's retained stable
    // anchor (or the occurrence element the toolbar is anchored to) is the callback anchor.
    store.setEditingMode('edit', event.nativeEvent, event.currentTarget, stableAnchorRef.current ?? anchor ?? undefined);
  };

  // Mirrors `FormContent`'s delete: recurring events open the scope dialog (which closes the surface
  // on submit); single events delete immediately and close.
  const handleDelete = () => {
    if (areRecurringEventsAvailable && recurringEventsPlugin && occurrence.displayTimezone.rrule) {
      store.deleteRecurringEvent({
        occurrenceStart: occurrence.displayTimezone.start.value,
        eventId: occurrence.id,
        onSubmit: stopEditing
      });
      return;
    }
    store.deleteEvent(occurrence.id);
    stopEditing();
  };
  return /*#__PURE__*/(0, _jsxRuntime.jsxs)(EventToolbarRoot, {
    className: classes.eventToolbar,
    elevation: 3,
    role: "toolbar",
    "aria-label": localeText.eventActionsToolbarAriaLabel,
    children: [/*#__PURE__*/(0, _jsxRuntime.jsx)(EventToolbarButton, {
      className: classes.eventToolbarEditButton,
      "aria-label": localeText.editEventButtonAriaLabel,
      onClick: handleEdit,
      children: /*#__PURE__*/(0, _jsxRuntime.jsx)(_EditRounded.default, {
        fontSize: "small"
      })
    }), /*#__PURE__*/(0, _jsxRuntime.jsx)(EventToolbarButton, {
      className: classes.eventToolbarDeleteButton,
      "aria-label": localeText.deleteEventButtonAriaLabel,
      onClick: handleDelete,
      children: /*#__PURE__*/(0, _jsxRuntime.jsx)(_DeleteRounded.default, {
        fontSize: "small"
      })
    })]
  });
}