"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.eventCalendarViewSelectors = void 0;
const eventCalendarViewSelectors = exports.eventCalendarViewSelectors = {
  view: state => state.view,
  views: state => state.views,
  hasDayView: state => state.views.includes('day'),
  /**
   * The user configuration for a time-grid based view (`day` or `week`), or `null` when none is set.
   */
  timeGridConfig: (state, view) => state.viewConfig?.[view] ?? null
};