"use strict";
'use client';

var _interopRequireDefault = require("@babel/runtime/helpers/interopRequireDefault").default;
var _interopRequireWildcard = require("@babel/runtime/helpers/interopRequireWildcard").default;
Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.CalendarGridTimeEventResizeHandler = void 0;
var _extends2 = _interopRequireDefault(require("@babel/runtime/helpers/extends"));
var _objectWithoutPropertiesLoose2 = _interopRequireDefault(require("@babel/runtime/helpers/objectWithoutPropertiesLoose"));
var React = _interopRequireWildcard(require("react"));
var _useStableCallback = require("@base-ui/utils/useStableCallback");
var _useRenderElement = require("@base-ui/react/internals/useRenderElement");
var _useEventResizeHandler = require("../../internals/utils/useEventResizeHandler");
var _useEventPointerResizeHandler = require("../../internals/utils/useEventPointerResizeHandler");
var _resizeUtils = require("../../internals/utils/resize-utils");
var _eventUtils = require("../../internals/utils/event-utils");
var _CalendarGridTimeColumnContext = require("../time-column/CalendarGridTimeColumnContext");
var _CalendarGridTimeEventContext = require("../time-event/CalendarGridTimeEventContext");
const _excluded = ["className", "render", "style", "side"];
// Time grid events are never all-day; keep them that way on resize.
function addPropertiesToResizedEvent() {
  return {
    allDay: false
  };
}
const CalendarGridTimeEventResizeHandler = exports.CalendarGridTimeEventResizeHandler = /*#__PURE__*/React.forwardRef(function CalendarGridTimeEventResizeHandler(componentProps, forwardedRef) {
  const {
      // Rendering props

      // Internal props
      side
      // Props forwarded to the DOM element
    } = componentProps,
    elementProps = (0, _objectWithoutPropertiesLoose2.default)(componentProps, _excluded);

  // Context hooks
  const contextValue = (0, _CalendarGridTimeEventContext.useCalendarGridTimeEventContext)();
  const {
    getDateAtPointer
  } = (0, _CalendarGridTimeColumnContext.useCalendarGridTimeColumnContext)();

  // Ref hooks
  const ref = React.useRef(null);

  // Feature hooks
  const getDragData = (0, _useStableCallback.useStableCallback)(input => (0, _extends2.default)({}, contextValue.getSharedDragData(input), {
    source: 'CalendarGridTimeEventResizeHandler',
    side
  }));

  // Pointer-resize session, built from the event's drag data. The pointer maps directly to a date,
  // so no grab offset is needed.
  const getResizeSession = (0, _useStableCallback.useStableCallback)(() => {
    const data = contextValue.getSharedDragData();
    return {
      start: data.start,
      end: data.end,
      eventId: data.eventId,
      occurrenceKey: data.occurrenceKey,
      originalOccurrence: data.originalOccurrence,
      resourceId: (0, _eventUtils.getPrimaryResourceId)(data.originalOccurrence.resource) ?? null
    };
  });

  // Shared by both resize handlers running together: native drag-and-drop serves the mouse, the
  // pointer handler serves touch/pen, so one handle resizes from whatever pointer the user has.
  const enabled = (0, _resizeUtils.isResizeHandlerEnabled)({
    side,
    isEventStartClipped: contextValue.isEventStartClipped,
    isEventEndClipped: contextValue.isEventEndClipped
  });
  const {
    state
  } = (0, _useEventResizeHandler.useEventResizeHandler)({
    ref,
    side,
    enabled,
    getDragData
  });
  (0, _useEventPointerResizeHandler.useEventPointerResizeHandler)({
    ref,
    side,
    enabled,
    surfaceType: 'time-grid',
    getDateAtPointer,
    getResizeSession,
    addPropertiesToResizedEvent
  });
  return (0, _useRenderElement.useRenderElement)('div', componentProps, {
    enabled,
    state,
    ref: [forwardedRef, ref],
    props: [elementProps]
  });
});
if (process.env.NODE_ENV !== "production") CalendarGridTimeEventResizeHandler.displayName = "CalendarGridTimeEventResizeHandler";