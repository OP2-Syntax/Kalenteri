'use client';

import _extends from "@babel/runtime/helpers/esm/extends";
import _objectWithoutPropertiesLoose from "@babel/runtime/helpers/esm/objectWithoutPropertiesLoose";
const _excluded = ["className", "render", "style", "side"];
import * as React from 'react';
import { useStableCallback } from '@base-ui/utils/useStableCallback';
import { useRenderElement } from '@base-ui/react/internals/useRenderElement';
import { useEventResizeHandler } from "../../internals/utils/useEventResizeHandler.mjs";
import { isResizeHandlerEnabled } from "../../internals/utils/resize-utils.mjs";
import { useCalendarGridDayEventContext } from "../day-event/CalendarGridDayEventContext.mjs";
// Day-grid resize is native drag only; touch/pointer resize is a follow-up (add it like the time grid
// via `useEventPointerResizeHandler` with a horizontal `getDateAtPointer`). Only `enabled` is shared.
export const CalendarGridDayEventResizeHandler = /*#__PURE__*/React.forwardRef(function CalendarGridDayEventResizeHandler(componentProps, forwardedRef) {
  const {
      // Rendering props

      // Internal props
      side
      // Props forwarded to the DOM element
    } = componentProps,
    elementProps = _objectWithoutPropertiesLoose(componentProps, _excluded);

  // Context hooks
  const contextValue = useCalendarGridDayEventContext();

  // Ref hooks
  const ref = React.useRef(null);

  // Feature hooks
  const getDragData = useStableCallback(input => _extends({}, contextValue.getSharedDragData(input), {
    source: 'CalendarGridDayEventResizeHandler',
    side
  }));
  const enabled = isResizeHandlerEnabled({
    side,
    isEventStartClipped: contextValue.isEventStartClipped,
    isEventEndClipped: contextValue.isEventEndClipped
  });
  const {
    state
  } = useEventResizeHandler({
    ref,
    side,
    enabled,
    getDragData
  });
  return useRenderElement('div', componentProps, {
    enabled,
    state,
    ref: [forwardedRef, ref],
    props: [elementProps]
  });
});
if (process.env.NODE_ENV !== "production") CalendarGridDayEventResizeHandler.displayName = "CalendarGridDayEventResizeHandler";