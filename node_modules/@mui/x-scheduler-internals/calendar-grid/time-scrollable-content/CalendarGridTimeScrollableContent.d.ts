import * as React from 'react';
import type { BaseUIComponentProps } from '@base-ui/react/internals/types';
export declare const CalendarGridTimeScrollableContent: React.ForwardRefExoticComponent<Omit<CalendarGridTimeScrollableContent.Props, "ref"> & React.RefAttributes<HTMLDivElement>>;
export declare namespace CalendarGridTimeScrollableContent {
  interface State {}
  interface Props extends BaseUIComponentProps<'div', State> {
    /**
     * Ref to the element that actually scrolls, where the drag-and-drop auto-scroll is registered. Defaults to this component's rendered element.
     */
    scrollableRef?: React.RefObject<HTMLElement | null>;
  }
}