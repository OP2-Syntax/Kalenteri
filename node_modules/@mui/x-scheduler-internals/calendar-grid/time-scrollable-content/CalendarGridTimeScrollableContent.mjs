'use client';

import _objectWithoutPropertiesLoose from "@babel/runtime/helpers/esm/objectWithoutPropertiesLoose";
const _excluded = ["className", "render", "style", "scrollableRef"];
import * as React from 'react';
import { useRenderElement } from '@base-ui/react/internals/useRenderElement';
import { CompositeList } from '@base-ui/react/internals/composite';
import { useAutoScrollForTimeGrid } from "../../use-auto-scroll-for-time-grid/index.mjs";
import { CalendarGridCellsRefsContext } from "../../internals/utils/CalendarGridCellsRefsContext.mjs";
import { jsx as _jsx } from "react/jsx-runtime";
export const CalendarGridTimeScrollableContent = /*#__PURE__*/React.forwardRef(function CalendarGridScrollableContent(componentProps, forwardedRef) {
  const {
      // Rendering props

      // Internal props
      scrollableRef
      // Props forwarded to the DOM element
    } = componentProps,
    elementProps = _objectWithoutPropertiesLoose(componentProps, _excluded);
  const ownRef = React.useRef(null);
  const columnsRefs = React.useRef([]);
  useAutoScrollForTimeGrid(scrollableRef ?? ownRef);
  const element = useRenderElement('div', componentProps, {
    ref: [forwardedRef, ownRef],
    props: [elementProps]
  });
  return /*#__PURE__*/_jsx(CompositeList, {
    elementsRef: columnsRefs,
    children: /*#__PURE__*/_jsx(CalendarGridCellsRefsContext.Provider, {
      value: columnsRefs,
      children: element
    })
  });
});
if (process.env.NODE_ENV !== "production") CalendarGridTimeScrollableContent.displayName = "CalendarGridTimeScrollableContent";