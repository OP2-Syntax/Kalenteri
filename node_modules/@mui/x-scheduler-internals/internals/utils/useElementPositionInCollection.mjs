import * as React from 'react';
import { useAdapterContext } from "../../use-adapter-context/index.mjs";
import { dateToTimelineAxisOffsetMs, getTimelineAxisDurationMs, isStartMinuteOutsideAxisWindow, isEndMinuteOutsideAxisWindow } from "./timeline-axis.mjs";
const MINUTE_MS = 60_000;
export function useElementPositionInCollection(parameters) {
  const {
    start,
    end,
    collection,
    durationMs
  } = parameters;
  // Deconstructed so an inline collection object stays memoization-friendly.
  const {
    start: collectionStart,
    end: collectionEnd,
    dayStartMinute,
    dayEndMinute
  } = collection;
  const adapter = useAdapterContext();
  return React.useMemo(() => computeElementPositionInCollection(adapter, {
    start,
    end,
    collection: {
      start: collectionStart,
      end: collectionEnd,
      dayStartMinute,
      dayEndMinute
    },
    durationMs
  }), [adapter, start, end, collectionStart, collectionEnd, dayStartMinute, dayEndMinute, durationMs]);
}

/**
 * Pure helper behind `useElementPositionInCollection`, callable outside React.
 */
export function computeElementPositionInCollection(adapter, parameters) {
  const {
    start,
    end,
    collection
  } = parameters;

  // The processed bounds already carry their wall-clock time of day. The offsets are
  // monotonic in the date (day index × day size + a clamped in-day term), so no
  // midnight-wrap correction is needed for `end >= start` inputs.
  const startOffsetMs = dateToTimelineAxisOffsetMs(adapter, collection, start.value, start.minutesInDay * MINUTE_MS);
  const endOffsetMs = dateToTimelineAxisOffsetMs(adapter, collection, end.value, end.minutesInDay * MINUTE_MS);
  const totalMs = parameters.durationMs ?? getTimelineAxisDurationMs(adapter, collection);
  const clampToTimeline = value => Math.min(Math.max(value, 0), totalMs);
  const clampedStartMs = clampToTimeline(startOffsetMs);
  const clampedEndMs = clampToTimeline(endOffsetMs);

  // A bound clamped in either direction means part of the element is hidden.
  const startingBeforeEdge = startOffsetMs < 0 || isStartMinuteOutsideAxisWindow(collection, start.minutesInDay);
  const endingAfterEdge = endOffsetMs > totalMs || isEndMinuteOutsideAxisWindow(collection, end.minutesInDay);
  return {
    position: clampedStartMs / totalMs,
    duration: Math.max(0, clampedEndMs - clampedStartMs) / totalMs,
    startingBeforeEdge,
    endingAfterEdge
  };
}