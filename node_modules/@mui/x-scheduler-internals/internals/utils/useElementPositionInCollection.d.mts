import type { Adapter } from "../../use-adapter/useAdapter.types.mjs";
import type { SchedulerProcessedDate } from "../../models/index.mjs";
import type { TimelineAxis } from "./timeline-axis.mjs";
export declare function useElementPositionInCollection(parameters: useElementPositionInCollection.Parameters): useElementPositionInCollection.ReturnValue;
/**
 * Pure helper behind `useElementPositionInCollection`, callable outside React.
 */
export declare function computeElementPositionInCollection(adapter: Adapter, parameters: useElementPositionInCollection.Parameters): useElementPositionInCollection.ReturnValue;
export declare namespace useElementPositionInCollection {
  interface Parameters {
    start: SchedulerProcessedDate;
    end: SchedulerProcessedDate;
    /**
     * The displayed range and daily hour window the element is positioned in: a
     * time-grid column (`viewConfig`) or the timeline's axis (`presetConfig`).
     */
    collection: TimelineAxis;
    /**
     * Precomputed `getTimelineAxisDurationMs(adapter, collection)`, skipping the
     * per-call derivation when the caller already holds it.
     */
    durationMs?: number;
  }
  interface ReturnValue {
    position: number;
    duration: number;
    startingBeforeEdge: boolean;
    endingAfterEdge: boolean;
  }
}