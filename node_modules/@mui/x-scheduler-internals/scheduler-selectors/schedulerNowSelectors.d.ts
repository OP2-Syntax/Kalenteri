import type { TemporalSupportedObject } from "../models/index.js";
import type { SchedulerState as State } from "../internals/utils/SchedulerStore/SchedulerStore.types.js";
export declare const schedulerNowSelectors: {
  showCurrentTimeIndicator: (state: State) => boolean;
  nowUpdatedEveryMinute: (state: State) => Date;
  isCurrentDay: (state: State, date: TemporalSupportedObject) => boolean;
};