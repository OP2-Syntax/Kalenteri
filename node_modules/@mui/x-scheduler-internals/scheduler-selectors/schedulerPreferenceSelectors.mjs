import _extends from "@babel/runtime/helpers/esm/extends";
import { createSelectorMemoized } from '@base-ui/utils/store';
import { DEFAULT_SCHEDULER_PREFERENCES } from "../internals/utils/SchedulerStore/index.mjs";
const allSchedulerPreferencesSelector = createSelectorMemoized(state => state.preferences, preferences => _extends({}, DEFAULT_SCHEDULER_PREFERENCES, preferences));
export const schedulerPreferenceSelectors = {
  all: allSchedulerPreferencesSelector,
  ampm: state => allSchedulerPreferencesSelector(state).ampm,
  weekStartsOn: state => allSchedulerPreferencesSelector(state).weekStartsOn
};